CREATE TABLE [dbo].[wz_TitleCurrency] (
    [ID]     INT           IDENTITY (1, 1) NOT NULL,
    [Conten] VARCHAR (500) NOT NULL,
    CONSTRAINT [PK_wz_TitleCurrency] PRIMARY KEY CLUSTERED ([ID] ASC)
);


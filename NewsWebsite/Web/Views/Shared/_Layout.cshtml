<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="description" content="寻味网-汇集各类时事、娱乐、游戏、体育和综艺等内容为一体的资讯网站，并根据用户浏览足迹提供相关的优惠商品，想了解更多好玩有趣的东西，就上寻味网！" />
    <link href="~/Content/css/main.css" rel="stylesheet" />
    <link href="~/Content/css/log.css" rel="stylesheet" />
    <title>@ViewBag.Title</title>
    <link rel="shortcut icon" href="~/Content/img/top.jpg" type="image/x-icon">
    <style>
        .onshow {
            color: red;
        }

        .logreg {
            z-index: 1;
            background-color: #e2e2e2;
            padding-bottom: 10px;
            width: 450px;
            height: auto;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%,-50%);
            display: none;
            border-radius: 5px;
            box-shadow: 0px 0px 20px rgba(70, 70, 70, 0.80);
        }
    </style>
    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/QRcode/jquery.qrcode.min.js"></script>
    <script type="text/javascript" src="http://pv.sohu.com/cityjson?ie=utf-8"></script>
    <script>
        $(function () {
            $.ajax({
                url: '/Home/VCAdd',
                type: 'POST',
                data: {
                    IP: returnCitySN.cip
                }
            })

            jQuery('#qrcode').qrcode({
                render: "canvas",
                width: 150,
                height: 150,
                text: window.location.href
            });
        })
    </script>
    <script type="text/javascript">
        window.onload = function () {
            var oDiv = document.getElementById("typeInfo"),
                H = 0,
                Y = oDiv
            while (Y) {
                H += Y.offsetTop;
                Y = Y.offsetParent;
            }
            window.onscroll = function () {
                var s = document.body.scrollTop || document.documentElement.scrollTop
                if (s > H) {
                    oDiv.style = "position:fixed;top:0;"
                } else {
                    oDiv.style = ""
                }
            }
        }
    </script>
</head>
<body>
    @RenderBody()
</body>
</html>